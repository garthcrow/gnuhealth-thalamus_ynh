# Chemin du fichier de config thalamus (lecture via THALAMUS_CONFIG)
[server]
POSTGRESQL_URI = "{{ env.POSTGRESQL_URI }}"
# Ajouter ici d'autres options de sécurité (CORS, JWT, etc.) selon la doc officielle